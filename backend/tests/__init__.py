"""
Test Suite for Developer Dashboard Backend.

Uses pytest with pytest-asyncio for async test support.
Tests are organized by feature matching the app structure.

Running tests:
    pytest -v                    # Run all tests
    pytest -v tests/test_auth.py # Run specific file
    pytest --cov=app            # With coverage
"""
